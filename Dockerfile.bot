# Dockerfile для сборки основного Telegram-бота
FROM golang:1.23 AS builder
WORKDIR /app
COPY go.mod go.sum ./
RUN go mod download
COPY . .
RUN CGO_ENABLED=0 GOOS=linux go build -o /app/bot ./cmd/bot

FROM alpine:3.18
WORKDIR /app
COPY --from=builder /app/bot .
CMD ["./bot"]
