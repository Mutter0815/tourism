# Dockerfile для сборки бота поддержки
FROM golang:1.23 AS builder
WORKDIR /app
COPY go.mod go.sum ./
RUN go mod download
COPY . .
RUN CGO_ENABLED=0 GOOS=linux go build -o /app/support_bot ./cmd/support_bot

FROM alpine:3.18
WORKDIR /app
COPY --from=builder /app/support_bot .
CMD ["./support_bot"]
